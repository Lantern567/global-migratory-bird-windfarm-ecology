# Data policy

`raw/` 保存原始下载或获授权的数据，`interim/` 保存清洗和投影后的中间产物，`processed/` 保存可供模型直接读取的标准化数据。三类目录默认不进入Git，只保留目录占位符和公开的数据字典。

鸟类追踪数据必须保留来源、许可证、访问日期、个体匿名要求和再分发限制。工程仓库导出的AEP曲线应复制到本地输入目录或通过发布版本获取，生态代码不得直接读取对方仓库内部的临时输出路径。

